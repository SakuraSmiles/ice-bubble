# Ice Bubble Docker - Sensitive Config Templates
# Copy files from this directory, replace tokens, and rename (remove .template suffix)
# ⚠️ NEVER commit the actual config files (without .template) to version control!

# admin-config.json.template
# collector-openclaw-config.json.template
